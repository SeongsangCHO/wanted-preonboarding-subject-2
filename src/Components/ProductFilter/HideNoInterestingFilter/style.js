import styled from "styled-components";

export const Container = styled.div`
  padding: 3px;
    & input {
      transform: scale(1.5);
    }
    & span {
      font-size: 1.2rem;
      padding-left: 5px;
      line-height: 1.2rem;
    }
`;
